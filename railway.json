{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Backend service — skipping Next.js build'"
  },
  "deploy": {
    "startCommand": "npm run dev:server",
    "healthcheckPath": "/api/state",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
